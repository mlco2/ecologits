"""
EcoLogits integrations with third-party observability frameworks.

Each sub-package is an optional extra: importing it requires the corresponding
extra to be installed (e.g. `pip install ecologits[langsmith]`).
"""
